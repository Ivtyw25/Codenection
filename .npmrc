; Expo SDK 57 pins react@19.2.3, but expo-router pulls react-dom@19.3.0 which
; peers on react@^19.3.0. react-dom is web-only and unused by this app, so the
; strict-peer check is noise here. Without this, any subsequent `npm install`
; or `npx expo install` fails to resolve.
legacy-peer-deps=true
